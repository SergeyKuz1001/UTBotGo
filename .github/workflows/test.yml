name: Test ghrc
on: [push]
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - run: ./build_docker_image.sh ${{ secrets.GITHUB_TOKEN }}
      - run: make run
